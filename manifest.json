{
  "schemaVersion": 1,
  "id": "io.github.epicbagel.syncbar",
  "name": "Syncbar",
  "version": "0.1.0",
  "author": "Jon",
  "license": "MIT",
  "description": "Watch Syncthing from the Omarchy bar: folder sync state, how far behind you are, and whether any peer is actually connected.",
  "homepage": "https://github.com/epicbagel/syncbar",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Syncbar",
    "description": "Syncthing folder state, sync progress and peer connectivity.",
    "category": "System",
    "aliases": [
      "syncthing",
      "sync"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "showLabel": true,
      "pollSeconds": 5,
      "warnDisconnected": false
    },
    "schema": [
      {
        "key": "showLabel",
        "type": "boolean",
        "label": "Show state on the bar",
        "defaultValue": true,
        "description": "Off leaves just the icon, coloured by state."
      },
      {
        "key": "warnDisconnected",
        "type": "boolean",
        "label": "Treat an absent peer as a fault",
        "defaultValue": false,
        "description": "Off by default. A peer being offline is a fact, not an error - a laptop is off most of the time. Turn this on only if a peer is meant to be always up, such as a server."
      },
      {
        "key": "pollSeconds",
        "type": "integer",
        "label": "Refresh every (seconds)",
        "min": 2,
        "max": 120,
        "step": 1,
        "defaultValue": 5,
        "description": "Syncthing's API is local and answers in milliseconds, so this can be brisk."
      }
    ]
  }
}
